# --- 1. 대용량 데이터 파일 (전체 무시) ---
*.jsonl
*.csv
# (단, requirements.txt나 설명용 txt는 예외로 하고 싶다면 아래 !로 예외 처리 가능)
# 하지만 review_analysis_result.txt 같은 데이터성 파일이 보여서 txt도 포함했습니다.
# 꼭 필요한 txt가 있다면 그 파일만 git add -f로 강제 추가하면 됩니다.

# --- 2. 압축 파일 (전체 무시) ---
*.tar.gz
*.tar
*.zip
*.gz

# --- 3. Python 및 시스템 자동 생성 파일 ---
__pycache__/
*.pyc
.DS_Store
.vscode/
venv/
env/

# --- 4. 특정 폴더 통째로 무시 (선택 사항) ---
# 만약 crawl 내의 데이터 폴더들을 통째로 무시하고 싶다면 아래 주석 해제
# crawl/naver/naver_reviews_split/
# crawl/tmdb/tmdb_data/split_tmdb/
